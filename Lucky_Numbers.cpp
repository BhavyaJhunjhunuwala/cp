#include<bits/stdc++.h>
#define ll long long

using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    ll n;
    cin>>n;
    // string ans;
    ll ans=(2*(pow(2,n)-1));
    cout<<ans<<endl;
    return 0;
}